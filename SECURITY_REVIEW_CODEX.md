# pocket-todo セキュリティレビュー（第三者監査）

監査日: 2026-09-19 / 対象: 作業ツリー上の `src/*.ts`、`wrangler.jsonc`、`tests/`。補助的に `package.json`、`package-lock.json`、`.gitignore`、`playwright.config.ts` を確認した。行番号は監査時点のもの。

## 結論と監査範囲

**High 2件、Medium 6件、Low 3件。** 優先対応は、シークレット未設定時の認証拒否、通知トピックのアクセス制御、手動リマインドの制限である。固定Bearerトークンを知る者は両APIを利用でき、現在の制限は課金・通知・外部API利用の総量を保証しない。

監査開始時から `.dev.vars.example`、`README.md`、`src/index.ts`、`src/notion.ts`、`wrangler.jsonc` に変更、`REVIEW.md`、`src/remind.ts` に未追跡状態があった。本報告は未コミット変更を含む現状を評価し、既存レビューの結論には依存していない。

ソースは変更していない。本番APIへのアクセス、実タスク作成、通知送信、デプロイ、シークレット値の読み出しは行っていない。本番のCloudflare Access/WAF、ntfy ACL、トークン強度、Notion権限、ログ内容・保存期間、Git履歴は未検証である。コードで確認できない運用対策の有無は断定しない。

重要度は、High＝認証境界または機密性への重大な影響、Medium＝条件付きの悪用・課金増加・可用性や完全性への影響、Low＝限定的影響または防御・検証不足とした。各項目の成立条件も併記する。

## High

### H-01 シークレット未設定時、既知の文字列で認証できる

- **根拠:** `src/index.ts:7-14,23-25,45-48`、`wrangler.jsonc:27-30`。
- **内容:** `AUTH_TOKEN` の存在・型を実行時に検証せずテンプレート文字列へ展開する。Bindingが存在しなければ期待値は `Bearer undefined` となる。TypeScriptの必須型はデプロイ先の設定を保証しない。
- **成立条件・影響:** シークレット未登録の環境が公開され、他の外部API設定が有効な場合、第三者が既知のヘッダーでタスク登録・リマインド実行に到達できる。正常に設定された本番で同じバイパスが起こるとは主張しない。
- **検証:** 実際のハンドラーをメモリ上で実行し、`AUTH_TOKEN: undefined`、`Authorization: Bearer undefined` で `/api/remind` が200を返し、モックNotion queryへ到達した。`/api/tasks` も同じ比較式を使用する。
- **改善案:** 認証比較前に非空・適切な型のシークレットを確認し、不備なら503等で必ず拒否する。デプロイ時にも必須Binding検査を入れる。未設定・空値・不正値を両ルートのテストに追加する。

### H-02 タスク名・期限の通知が、トピック名依存のアクセス制御になる

- **根拠:** `src/remind.ts:13,18-39`、`src/index.ts:79-83`。
- **内容:** Notionから取得したタスク名・期限を `https://ntfy.sh` に送るが、送信に認証情報がない。機密性はトピック側のACLや名前の秘匿に依存する。
- **成立条件・影響:** 公開・未保護トピックを使用し、その名前が推測・流出した場合、APIのBearerトークンを持たない第三者も通知を購読できる。書き込みも公開なら偽通知が可能。業務名・顧客名等をタイトルに含めると情報漏洩となる。実際のトピック名、ACL、漏洩発生は未確認であり、非公開購読ACLが別途設定済みなら評価を下げられる。
- **改善案:** 購読・投稿とも明示的なACLと認証で保護したトピックを利用し、投稿用資格情報をWorkers Secretに置く。第三者サービスに送る内容は件数などへ最小化する。長いランダムなトピック名は補助策とし、`NTFY_TOPIC` も機密設定として扱う。
- **仕様確認:** [ntfy公式FAQ](https://docs.ntfy.sh/faq/) はACLがない場合にトピック名がパスワード相当になると説明している。[認証・アクセス制御](https://docs.ntfy.sh/config/)も参照。

## Medium

### M-01 `/api/remind` がレート制限・同時実行制限を迂回する

- **根拠:** `src/index.ts:33-38,45-54`、`src/remind.ts:13,31-40`。
- **内容:** `RATE_LIMITER` は `/api/tasks` のみで呼ばれる。手動リマインドは毎回Notion query、対象がある場合はntfy投稿を行う。短時間の重複抑止もない。
- **成立条件・影響:** 固定トークンを知る者、またはH-01の成立時に、通知連打やNotion利用枠の消費が可能。正常なタスク登録・定期通知にも影響する。Anthropicはこのルートで呼ばれず、直接のAI課金増加ではない。
- **検証:** モック環境で連続7回がすべて200。リミッター呼び出しは0回、各回でNotion queryが発生した。通知対象なしの応答で検証したため実通知の連打は行っていない。
- **改善案:** 手動実行を管理者専用資格情報に分離するか本番では無効化する。認証後の実行頻度制限、実行中ロック、日付・内容に基づく重複通知抑止を加える。cronとの競合も考慮する。

### M-02 `global` キーでも全拠点合計の上限・課金上限にはならない

- **根拠:** `src/index.ts:33-40`、`wrangler.jsonc:15-24`、`src/classify.ts:27-43`。
- **内容:** Cloudflare Rate Limiting Bindingのカウンターはキーごと・Cloudflare拠点ごとであり、結果整合で厳密な会計用途ではない。`global` という文字列は全世界での一元カウンターを意味しない。
- **成立条件・影響:** 有効トークンを持つ攻撃者が複数拠点に到達する経路を使えば、総量は5件/分を超え得る。同一拠点でも厳密な上限ではない。加えて毎分制限だけでは日次支出を止めず、1受理につきSDKの再試行や長い入力があり、API呼び出し数・費用の上限にもならない。同じ拠点内で単にIPやヘッダーを変えるだけで新しいキーが作られる実装ではない。
- **改善案:** 現在のBindingは局所的な減速策として残し、厳密な予算が必要なら単一Durable Object等で日次件数・入力量・同時実行数を管理する。プロバイダー側の支出制御とアラートを併用する。
- **仕様確認:** [Cloudflare Rate Limiting](https://developers.cloudflare.com/workers/runtime-apis/bindings/rate-limit/)。多拠点への実攻撃は行っていない。

### M-03 入力の型・サイズ制限がなく、検証処理はレート制限の外側

- **根拠:** `src/index.ts:27-35`、`src/classify.ts:5-15,20-21,33,42`、`src/notion.ts:55`、`src/page.ts:45`。
- **内容:** `json<{ text?: string }>()` は型検証を行わず、数値やオブジェクトの `text` で `.trim()` が例外になる。JSON全体の読み込み・パース・trimは制限前で、body、text、モデル出力titleに上限がない。`max_tokens: 2000` は生成側の上限であり、入力の制限ではない。
- **成立条件・影響:** 有効トークン所持者は、リミッターを消費しない不正・巨大入力でパースやログの負荷を増やせる。有効な長文はAI入力コストを増やす。分類失敗時は全文がNotionタイトルに渡り、Notionの文字数上限を超えて保存できなくなる。
- **検証:** `{ "text": 123 }` と `{ "text": {} }` は500、リミッター呼び出し0回。1万文字は202となった。巨大負荷試験は実施していない。
- **改善案:** JSONパース前に実際の読み取りバイト数を制限する（Content-Lengthだけに依存しない）。認証済み要求全体の粗い制限と、課金処理の制限を分ける。実行時スキーマで文字列・長さを検査し、400/413を返す。Notion向けタイトルとフォールバックにも上限を適用する。
- **仕様確認:** [Notion request limits](https://developers.notion.com/reference/request-limits)では `text.content` は2000文字。プラットフォームのbody制限があっても、この用途に適した上限の代わりにはならない。

### M-04 永続保存された単一Bearerトークンが全操作権限を持つ

- **根拠:** `src/page.ts:52-59,71-77`、`src/index.ts:23,46`、`wrangler.jsonc:28`。
- **内容:** 同じ固定トークンを両APIに使い、有効期限・端末別失効・権限分離がない。ブラウザーのlocalStorageへ永続保存し、正常利用中に消去するUIもない。トークン入力はマスクされない `prompt`。
- **成立条件・影響:** 同一オリジンのスクリプト実行、端末やブラウザープロファイルへのアクセス、画面の覗き見等で漏洩すると、Secretを交換するまで再利用できる。現行ページで直接成立するXSSは発見しておらず、localStorageを使うだけで外部サイトが読めるわけではない。
- **改善案:** 高エントロピーのランダム値を必須とし、交換手順・消去UI・管理権限分離を設ける。可能ならCloudflare Access等の利用者認証と短期セッションに移行する。Cookie方式に変更する場合はHttpOnly/Secure/SameSiteとCSRF対策も同時に設計する。
- **補足:** 現在は認証失敗要求の制限もない（`src/index.ts:23-25,46-48`）。弱い合言葉だとオンライン推測が可能なため、認証前の粗い制限を追加する。ただし十分にランダムな値の総当たり成功を示すものではない。

### M-05 外部APIの生エラーがログ・API応答へ伝播する

- **根拠:** `src/notion.ts:32-33,81-82`、`src/remind.ts:41-42`、`src/index.ts:52-53,63-64,70-72,81-82`。
- **内容:** Notion/ntfyのレスポンス本文をそのまま例外へ取り込み、console.errorまたは `/api/remind` の500 JSONへ流す。AnthropicのSDK例外も丸ごとログへ出す。
- **成立条件・影響:** 上流エラーにタスク内容、データソース識別子、トピック名等が含まれる場合に、ログ閲覧者やAPI呼び出し元へ広がる。生レスポンスを許すためデータ最小化が保証されない。APIキーそのものが現在のエラーに含まれることや、実際の漏洩は確認していない。
- **改善案:** 外部応答は分類したエラーコード、HTTPステータス、安全なrequest ID等へ変換する。利用者には一般化した失敗応答を返し、ログは長さ制限・機密値のマスキング・保存期間と閲覧権限を設定する。合成した機密文字列を上流エラーに含めるテストで非出力を確認する。

### M-06 バックグラウンド処理に永続性がなく、再試行も重複・枠消費を招く

- **根拠:** `src/index.ts:40-41,57-73`、`src/classify.ts:27-43`、`src/notion.ts:68-82`、`src/page.ts:83-85`。
- **内容:** 永続キューに格納する前に202を返し、フォームは成功表示と入力消去を行う。外部fetchに明示的なdeadlineがなく、Anthropic SDKの既定タイムアウトは10分、再試行は2回。HTTP応答後の `waitUntil` は最大30秒であり、完了を保証しない。Notion書き込みは全例外で即時に1回再試行し、冪等性の管理もない。
- **影響:** 外部APIの遅延や障害で受理済みタスクが失われ、利用者が気付けない。Notion側で書き込み済みでも応答が失われれば二重登録となり得る。400等の再試行は無駄な負荷を増やし、429の待機指示も尊重しない。主に可用性・完全性の問題で、任意コード実行ではない。
- **改善案:** 永続キューへの投入成功後に受理を返す。処理IDによる重複排除、失敗の保管・照会、サービス別のタイムアウト、上限付きバックオフとRetry-After対応を設ける。再試行可能な失敗と入力エラーを区別する。
- **仕様確認:** [Cloudflare実行コンテキスト](https://developers.cloudflare.com/workers/runtime-apis/context/)、[Anthropic TypeScript SDK](https://platform.claude.com/docs/en/cli-sdks-libraries/sdks/typescript)、[Notion request limits](https://developers.notion.com/reference/request-limits)。SDKの既定値はインストール済み実装でも確認した。

## Low

### L-01 フォームの埋め込み制限・CSPがコードにない

- **根拠:** `src/index.ts:18`、`src/page.ts:50-93`。
- **内容・影響:** CSP、`frame-ancestors` / X-Frame-Optionsを設定していない。ブラウザーが埋め込み内で保存トークンを利用できる状況では、クリックジャッキングによる意図しない操作の余地がある。第三者iframeのストレージ制限等に依存し、トークン読取や任意タスク作成が無条件に可能とはしない。CSPがないこと自体はXSSの証拠ではない。
- **改善案:** `frame-ancestors 'none'` をHTTP CSPで指定する。インラインscript/styleにハッシュやnonceを付け、`default-src`、`script-src`、`connect-src 'self'`、`base-uri 'none'` 等を実装に合わせて制限する。nosniffも加える。CDN側の設定は未確認。

### L-02 LLM出力の業務検証とリマインドのページネーションが不足

- **根拠:** `src/classify.ts:5-15,35-48`、`src/notion.ts:22-46,58-65`。
- **内容・影響:** dueは日付の書式だけを検証するため、実在しない日付もスキーマ上は通る。ユーザー文中の指示で分類結果を誘導することは可能で、構造化出力は意味の正しさを保証しない。またNotion queryの `has_more` / `next_cursor` を扱わず、大量の期限超過タスクがあると後続ページの通知が欠ける。主にデータ品質・通知の完全性の問題。
- **改善案:** 実在日付、タイトル長、タグ重複等をローカルで検証する。モデル出力を権限判断へ使わない。カーソルを扱いつつ最大処理量を設け、通知は要約・分割する。
- **境界:** モデルにAPIキーを渡すプロンプトやツール実行権限はなく、入力から外部送信先URLを指定できる構造もない。プロンプト注入からシークレット窃取・SSRFへ直結する経路は確認していない。

### L-03 セキュリティ回帰テストが不足し、実行先が本番に既定設定されている

- **根拠:** `tests/api.spec.ts:12-54`、`tests/ui.spec.ts:14-44`、`playwright.config.ts:3-11`。
- **内容・影響:** 認証の負例はtasksのみで、remind、Secret未設定、型・サイズ境界、429、ログの機密値、外部障害を検証しない。正しいトークンがない場合は認証後テストがskipされる。設定なしのE2Eは本番URLを使い、トークンを与えると実際にタスクを作る。また任意の `POCKET_TODO_URL` に実トークンを送れるため、CI設定の誤りで漏洩し得る。トレース等による現行の漏洩は確認していない。
- **改善案:** 外部APIをモックしたローカルテストと専用の検証環境を基本にする。両APIの未設定認証・不正入力・制限・エラー秘匿を追加する。本番E2Eは明示的な選択を必要とし、専用トークンと送信先検査を使う。認証後テストが全skipの実行を十分な検証と扱わない。

## シークレット、XSS、外部APIに関する確認事項

- 対象ソース・`wrangler.jsonc`・テストに、実値のAUTH_TOKEN、Anthropic APIキー、Notionトークンの直書きは見つからなかった。値はBinding/環境変数から取得する。`wrangler.jsonc:13` のデータソースIDは認証資格情報ではなく、それ単体をキー漏洩とは評価しない。
- `.gitignore:2` は `.dev.vars` を除外している。ただし過去のコミット、他のenvファイル、CI成果物、実ログまで漏洩なしとは保証しない。`wrangler.jsonc:27-30` のSecret一覧にはNTFY_TOPICがなく、通知設定を含めた運用文書の整備が望ましい。
- `src/page.ts:69,84,88` は `textContent` で表示し、動的な `innerHTML`、ユーザー入力のHTML展開、外部script読込はない。反射型・保存型・DOM型XSSの直接的な入力経路は確認できなかった。
- 認証はCookieではなくJSが付けるAuthorizationヘッダーで、コードに他オリジンを許可するCORS設定はない。通常の第三者フォームによるCSRFで必要なBearerヘッダーが自動付与されるわけではない。Origin検証がないことだけを認証回避として扱っていない。
- Notion/Anthropic/ntfyの宛先はHTTPSかつ固定であり、APIキーはブラウザーHTMLやモデルのユーザーメッセージへ埋め込まれない。タスク本文はAnthropic、分類結果はNotion、期限対象のタイトルはntfyへ送られる。サービスごとの保存・利用条件、Notion Integrationの最小権限、対象データソース限定の共有は運用確認事項。
- 文字列の `!==` は定数時間比較ではないが、ネットワーク越しの実用的なタイミング攻撃は未実証。まず設定検証・強い乱数トークン・試行制限を優先し、比較を共通化する際に定数時間方式を検討する。

## 依存関係の検査

ロックファイル上の主要バージョンは以下のとおり。

| パッケージ | バージョン | 根拠 |
| --- | --- | --- |
| @anthropic-ai/sdk | 0.127.0 | `package-lock.json:22` |
| hono | 4.13.8 | `package-lock.json:1414` |
| zod | 4.6.5 | `package-lock.json:1745` |
| wrangler | 4.135.0 | `package-lock.json:1662` |
| @playwright/test | 1.63.0 | `package-lock.json:1239` |

監査時点でlock内の105パッケージ名とバージョンを[npm Security Advisories Bulk API](https://registry.npmjs.org/-/npm/v1/security/advisories/bulk)へ照会し、応答は空オブジェクト（該当アドバイザリなし）だった。ソース・シークレットは送信していない。`npm install`、`npm audit fix` 等は実行していない。

これは当該データベースとの照合結果であり、未知の脆弱性・悪意あるパッケージ・全実行経路の安全性を保証しない。`package.json:14-22` は範囲指定なのでCIではlockを用いる `npm ci` を基本とし、定期的な依存更新とアドバイザリ確認を行う。古い最小指定 `hono ^4.6.0` だけから、現行lockに過去の脆弱性があるとは断定しない。

## 検証方法と限界

ソースをesbuildの `write:false` でメモリ上にバンドルし、Node.jsで実際のfetchハンドラーを呼び出した。外部fetchはすべて合成応答へ置き換え、資格情報はダミー値を使った。検証用ファイル、ビルド成果物、テストレポートは作成していない。

| 検証 | 結果 |
| --- | --- |
| AUTH_TOKEN未設定 + Bearer undefined → remind | 200、モックNotion queryへ到達 |
| textが数値・オブジェクト → tasks | 500、リミッター呼び出し0回 |
| textが1万文字 → tasks | 202、リミッター呼び出し1回 |
| 正常認証でremindを7回連続実行 | 全回200、リミッター呼び出し0回 |

ローカルのリミッターは常に許可するスタブであり、Cloudflare実環境での多拠点制限や障害挙動を再現したものではない。XSS/クリックジャッキングのブラウザー実証、本番侵入試験、負荷試験は実施していない。既存Playwrightテストは本番書き込みとファイル生成を避けるため実行しなかった。本監査で新規作成したファイルは本報告書のみ。
